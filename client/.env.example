# Leave unset for local dev — the Vite dev server proxies /api to the Express server,
# so the app works over the same origin from your computer or your phone (same Wi-Fi).
# VITE_API_BASE=http://localhost:3001
